{
  "id": "ground_taxi_clearance",
  "frequency_group": "ground",
  "slots": {
    "required": [
      "callsign_spelled",
      "runway_human"
    ],
    "optional": [
      "taxi_route",
      "instruction_code",
      "instruction_text"
    ]
  },
  "render": {
    "spanish": "{callsign_spelled}, {instruction_text}",
    "instruction_map": {
      "taxi_clearance": "ruede vía {taxi_route} al punto de espera pista {runway_human}",
      "hold_short": "mantenga posición antes de la pista {runway_human}"
    },
    "default_instruction_code": "taxi_clearance"
  },
  "metadata": {
    "default_instruction_code": "taxi_clearance",
    "allow_instruction_override": true
  },
  "fallback": {
    "defaults": {
      "instruction_code": "taxi_clearance",
      "taxi_route": "taxiway principal",
      "callsign": "TRAFICO",
      "callsign_spelled": "Tráfico",
      "runway_human": "uno cero"
    }
  }
}
